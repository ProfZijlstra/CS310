// Both NO and FALSE are 0
enum Boolean { NO, YES, FALSE = 0, TRUE = 1 };

// The numbers do not have to be consecutive
// And characters are of course numbers too
enum White_space {
    space = ' ',
    NEWLINE = '\n',
    TAB = '\t',
    RETURN = '\r'
};
